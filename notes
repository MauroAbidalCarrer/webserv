Moi:
    Fix post with folder.
    Fix Ctrl+C after post of chunked message.
Oscar:
    Implement ressource creation for chunked requests.
    Send default message (just) with status code.
test convoluted config file
    set multiple hosts on /etc/hots
test CGI that overflows pipe